diff --git a/src/app.rs b/src/app.rs
--- a/src/app.rs	2026-03-14 21:28:36.857414550 +0000
+++ b/src/app.rs	2026-03-14 21:35:45.805628159 +0000
@@ -44,7 +44,7 @@
         .add_plugins(default_plugins)
         .init_state::<GameState>()
         .insert_resource(Time::<Fixed>::from_hz(SIMULATION_HZ))
-        .insert_resource(ClearColor(Color::srgb(0.47, 0.28, 0.20)))
+        .insert_resource(ClearColor(Color::srgb(0.74, 0.53, 0.43)))
         .add_plugins((
             CorePlugin,
             WorldPlugin,
diff --git a/src/core/mod.rs b/src/core/mod.rs
--- a/src/core/mod.rs	2026-03-14 21:28:36.860252083 +0000
+++ b/src/core/mod.rs	2026-03-14 21:24:59.777573362 +0000
@@ -50,8 +50,8 @@
 impl Plugin for CorePlugin {
     fn build(&self, app: &mut App) {
         app.insert_resource(GlobalAmbientLight {
-            color: Color::srgb(0.64, 0.48, 0.41),
-            brightness: 920.0,
+            color: Color::srgb(0.71, 0.54, 0.46),
+            brightness: 760.0,
             ..default()
         })
         .insert_resource(CameraController::default())
@@ -67,8 +67,8 @@
 fn setup_scene(mut commands: Commands) {
     commands.spawn((
         DirectionalLight {
-            color: Color::srgb(1.0, 0.92, 0.84),
-            illuminance: 16_500.0,
+            color: Color::srgb(1.0, 0.94, 0.88),
+            illuminance: 14_500.0,
             shadows_enabled: true,
             ..default()
         },
@@ -78,6 +78,14 @@
 
     commands.spawn((
         Camera3d::default(),
+        DistanceFog {
+            color: Color::srgba(0.79, 0.55, 0.45, 0.94),
+            falloff: FogFalloff::Linear {
+                start: 96.0,
+                end: 240.0,
+            },
+            ..default()
+        },
         Transform::from_xyz(-32.0, 28.0, 30.0).looking_at(Vec3::ZERO, Vec3::Y),
         MainCamera,
     ));
diff --git a/src/world/decorations.rs b/src/world/decorations.rs
--- a/src/world/decorations.rs	2026-03-14 21:28:36.858425358 +0000
+++ b/src/world/decorations.rs	2026-03-14 21:21:05.109498384 +0000
@@ -12,6 +12,7 @@
 pub(crate) enum DecorationMaterial {
     Dark,
     Dust,
+    Salt,
 }
 
 #[derive(Clone, Debug, PartialEq)]
@@ -46,7 +47,6 @@
             let normal =
                 estimate_normal(seed.0, world_x as f64 + 0.5, world_y as f64 + 0.5, profile);
 
-            // Les cellules bloquees representent les gros blocs visibles et jouables.
             if cell.blocked {
                 decorations.push(make_boulder_spec(
                     local,
@@ -61,18 +61,19 @@
                 continue;
             }
 
-            // Ici je rajoute des petits eclats rocheux pour casser la monotonie visuelle
-            // des plaines et renforcer les bordures d'ejecta / de crateres.
-            let decorative_density = (0.004
-                + surface.rockiness * 0.030
-                + surface.ejecta * 0.032
-                + surface.crater_rim * 0.025
-                - surface.dust_cover * 0.020)
-                .clamp(0.0, 0.095);
+            let decorative_density = (0.002
+                + surface.rockiness * 0.026
+                + surface.ejecta * 0.028
+                + surface.crater_rim * 0.024
+                + surface.yardang_mask * 0.014
+                + surface.volcanic_mask * 0.008
+                - surface.dust_cover * 0.018
+                - surface.sediment_mask * 0.012)
+                .clamp(0.0, 0.090);
 
             if rand01(seed.0 ^ 0x4A11_5F1E, world_x, world_y) < decorative_density {
-                let count =
-                    1 + (rand01(seed.0 ^ 0x4411_8822, world_x, world_y) * 2.2).floor() as usize;
+                let count = 1
+                    + (rand01(seed.0 ^ 0x4411_8822, world_x, world_y) * 2.6).floor() as usize;
                 for index in 0..count {
                     decorations.push(make_rubble_spec(
                         local,
@@ -87,6 +88,27 @@
                     ));
                 }
             }
+
+            let salt_density = (surface.salts * 0.022
+                + surface.sediment_mask * 0.016
+                + surface.crater_floor * 0.010
+                - surface.rockiness * 0.012)
+                .clamp(0.0, 0.040);
+
+            if surface.salts > 0.34
+                && rand01(seed.0 ^ 0x515A_1711, world_x, world_y) < salt_density
+            {
+                decorations.push(make_salt_plate_spec(
+                    local,
+                    cell.height,
+                    world_x,
+                    world_y,
+                    &surface,
+                    normal,
+                    profile,
+                    seed,
+                ));
+            }
         }
     }
 
@@ -136,12 +158,19 @@
     profile: &PlanetProfile,
     seed: WorldSeed,
 ) -> SurfaceDecorationSpec {
-    let width = 0.78 + rand01(seed.0 ^ 0x0DD5_9A7E, world_x, world_y) * 1.12;
-    let depth = 0.64 + rand01(seed.0 ^ 0xB011_DA7A, world_y, world_x) * 0.96;
-    let vertical = 0.36 + rand01(seed.0 ^ 0x7A11_4B4D, world_x, world_y) * 0.58;
+    let yardang_align = surface.yardang_mask.clamp(0.0, 1.0);
+    let elongation = 1.0 + yardang_align * 1.4;
+    let compact = 1.0 - yardang_align * 0.30;
+    let width = (0.76 + rand01(seed.0 ^ 0x0DD5_9A7E, world_x, world_y) * 1.08) * elongation;
+    let depth = (0.60 + rand01(seed.0 ^ 0xB011_DA7A, world_y, world_x) * 0.90) * compact;
+    let vertical = 0.30
+        + rand01(seed.0 ^ 0x7A11_4B4D, world_x, world_y) * 0.54
+        + surface.rockiness * 0.18
+        + surface.yardang_mask * 0.06;
     let offset_x = rand_signed(seed.0 ^ 0x91B0_11D3, world_x, world_y) as f32 * 0.22;
     let offset_z = rand_signed(seed.0 ^ 0xA31D_0C7A, world_x, world_y) as f32 * 0.22;
-    let dust_bias = (surface.dust_cover * 0.72
+    let dust_bias = (surface.dust_cover * 0.60
+        + surface.sediment_mask * 0.12
         + rand01(seed.0 ^ 0xD057_BA51, world_x, world_y) * 0.28)
         .clamp(0.0, 1.0);
 
@@ -150,10 +179,21 @@
             local, height, profile, offset_x, offset_z, vertical, 0.12,
         ),
         scale: Vec3::new(width, vertical, depth),
-        yaw: rand01(seed.0 ^ 0x5EED_F00D, world_x, world_y) * std::f32::consts::TAU,
+        yaw: if yardang_align > 0.48 {
+            wind_yaw(profile.wind_direction)
+                + rand_signed(seed.0 ^ 0x5EED_F00D, world_x, world_y) as f32 * 0.28
+        } else {
+            rand01(seed.0 ^ 0x5EED_F00D, world_x, world_y) * std::f32::consts::TAU
+        },
         normal,
-        tilt_strength: (0.12 + surface.rockiness * 0.26 + surface.ejecta * 0.08).clamp(0.0, 0.42),
-        material: if dust_bias > 0.58 {
+        tilt_strength: (0.12
+            + surface.rockiness * 0.24
+            + surface.ejecta * 0.08
+            + surface.yardang_mask * 0.05)
+            .clamp(0.0, 0.44),
+        material: if surface.salts > 0.48 && surface.rockiness < 0.40 {
+            DecorationMaterial::Salt
+        } else if dust_bias > 0.60 {
             DecorationMaterial::Dust
         } else {
             DecorationMaterial::Dark
@@ -174,15 +214,21 @@
     seed: WorldSeed,
 ) -> SurfaceDecorationSpec {
     let index_i32 = index as i32;
-    let width = 0.14 + rand01(seed.0 ^ 0x2244_5011, world_x + index_i32, world_y) * 0.28;
-    let depth = 0.12 + rand01(seed.0 ^ 0x3311_7EED, world_y, world_x + index_i32) * 0.26;
-    let vertical = 0.08 + rand01(seed.0 ^ 0x9EED_A115, world_x, world_y + index_i32) * 0.16;
+    let yardang_align = surface.yardang_mask.clamp(0.0, 1.0);
+    let width = (0.12 + rand01(seed.0 ^ 0x2244_5011, world_x + index_i32, world_y) * 0.28)
+        * (1.0 + yardang_align * 0.45);
+    let depth = (0.10 + rand01(seed.0 ^ 0x3311_7EED, world_y, world_x + index_i32) * 0.22)
+        * (1.0 - yardang_align * 0.18);
+    let vertical = 0.07
+        + rand01(seed.0 ^ 0x9EED_A115, world_x, world_y + index_i32) * 0.14
+        + surface.rockiness * 0.04;
     let offset_x =
         rand_signed(seed.0 ^ 0x0199_1A5E, world_x + index_i32 * 3, world_y) as f32 * 0.42;
     let offset_z =
         rand_signed(seed.0 ^ 0xC4A7_115E, world_x, world_y + index_i32 * 5) as f32 * 0.42;
-    let dust_mix = (surface.dust_cover * 0.78
-        + rand01(seed.0 ^ 0xDADA_7711, world_x + index_i32, world_y) * 0.22)
+    let dust_mix = (surface.dust_cover * 0.70
+        + surface.sediment_mask * 0.12
+        + rand01(seed.0 ^ 0xDADA_7711, world_x + index_i32, world_y) * 0.18)
         .clamp(0.0, 1.0);
 
     SurfaceDecorationSpec {
@@ -190,10 +236,18 @@
             local, height, profile, offset_x, offset_z, vertical, 0.18,
         ),
         scale: Vec3::new(width, vertical, depth),
-        yaw: rand01(seed.0 ^ 0x551A_0042, world_x + index_i32, world_y) * std::f32::consts::TAU,
+        yaw: if yardang_align > 0.58 {
+            wind_yaw(profile.wind_direction)
+                + rand_signed(seed.0 ^ 0x551A_0042, world_x + index_i32, world_y) as f32 * 0.32
+        } else {
+            rand01(seed.0 ^ 0x551A_0042, world_x + index_i32, world_y) * std::f32::consts::TAU
+        },
         normal,
-        tilt_strength: (0.16 + surface.rockiness * 0.18).clamp(0.0, 0.50),
-        material: if dust_mix > 0.68 && surface.rockiness < 0.58 {
+        tilt_strength: (0.14 + surface.rockiness * 0.16 + surface.yardang_mask * 0.08)
+            .clamp(0.0, 0.50),
+        material: if surface.salts > 0.44 && surface.rockiness < 0.34 {
+            DecorationMaterial::Salt
+        } else if dust_mix > 0.70 && surface.basalt_exposure < 0.42 {
             DecorationMaterial::Dust
         } else {
             DecorationMaterial::Dark
@@ -202,6 +256,35 @@
     }
 }
 
+fn make_salt_plate_spec(
+    local: UVec2,
+    height: f32,
+    world_x: i32,
+    world_y: i32,
+    surface: &super::generation::SurfaceSample,
+    normal: Vec3,
+    profile: &PlanetProfile,
+    seed: WorldSeed,
+) -> SurfaceDecorationSpec {
+    let width = 0.18 + rand01(seed.0 ^ 0xBADA_0011, world_x, world_y) * 0.28;
+    let depth = 0.16 + rand01(seed.0 ^ 0xBADA_0012, world_y, world_x) * 0.24;
+    let vertical = 0.03 + rand01(seed.0 ^ 0xBADA_0013, world_x, world_y) * 0.05;
+    let offset_x = rand_signed(seed.0 ^ 0xBADA_0014, world_x, world_y) as f32 * 0.30;
+    let offset_z = rand_signed(seed.0 ^ 0xBADA_0015, world_y, world_x) as f32 * 0.30;
+
+    SurfaceDecorationSpec {
+        local_translation: rock_local_translation(
+            local, height, profile, offset_x, offset_z, vertical, 0.35,
+        ),
+        scale: Vec3::new(width, vertical, depth),
+        yaw: rand01(seed.0 ^ 0xBADA_0016, world_x, world_y) * std::f32::consts::TAU,
+        normal,
+        tilt_strength: (0.08 + surface.sediment_mask * 0.08).clamp(0.0, 0.20),
+        material: DecorationMaterial::Salt,
+        shape_seed: compose_shape_seed(seed.0 ^ 0xBADA_0017, world_x, world_y, 17),
+    }
+}
+
 fn rock_local_translation(
     local: UVec2,
     height: f32,
@@ -315,8 +398,8 @@
     ];
 
     let min_axis = scale.x.min(scale.y).min(scale.z);
-    let radial_amount = min_axis * 0.24;
-    let lateral_amount = min_axis * 0.16;
+    let radial_amount = min_axis * 0.18;
+    let lateral_amount = min_axis * 0.14;
 
     let mut corners = [Vec3::ZERO; 8];
     for (index, corner) in base.into_iter().enumerate() {
@@ -324,16 +407,18 @@
         let radial = (hash_unit(shape_seed, index as u64) * 2.0 - 1.0) * radial_amount;
         let lateral = Vec3::new(
             (hash_unit(shape_seed ^ 0x1111_AAAA, index as u64) * 2.0 - 1.0) * lateral_amount,
-            (hash_unit(shape_seed ^ 0x2222_BBBB, index as u64) * 2.0 - 1.0) * lateral_amount * 0.65,
+            (hash_unit(shape_seed ^ 0x2222_BBBB, index as u64) * 2.0 - 1.0)
+                * lateral_amount
+                * 0.55,
             (hash_unit(shape_seed ^ 0x3333_CCCC, index as u64) * 2.0 - 1.0) * lateral_amount,
         );
 
         let mut deformed = corner + sign * radial + lateral;
 
         if corner.y > 0.0 {
-            deformed.y *= 0.84 + hash_unit(shape_seed ^ 0x4444_DDDD, index as u64) * 0.34;
+            deformed.y *= 0.86 + hash_unit(shape_seed ^ 0x4444_DDDD, index as u64) * 0.28;
         } else {
-            deformed.y *= 0.70 + hash_unit(shape_seed ^ 0x5555_EEEE, index as u64) * 0.14;
+            deformed.y *= 0.72 + hash_unit(shape_seed ^ 0x5555_EEEE, index as u64) * 0.12;
         }
 
         corners[index] = deformed;
@@ -346,3 +431,8 @@
 fn hash_unit(seed: u64, index: u64) -> f32 {
     unit_f64_from_hash(splitmix64(seed ^ index.wrapping_mul(0x9E37_79B9_7F4A_7C15))) as f32
 }
+
+#[inline]
+fn wind_yaw(direction: Vec2) -> f32 {
+    direction.y.atan2(direction.x)
+}
diff --git a/src/world/generation.rs b/src/world/generation.rs
--- a/src/world/generation.rs	2026-03-14 21:28:36.858901736 +0000
+++ b/src/world/generation.rs	2026-03-14 21:16:54.784705653 +0000
@@ -1,8 +1,8 @@
 use bevy::prelude::*;
 
 use super::bruit::{
-    billow_fbm, directional_ripples, domain_warp, fbm, rand01, remap_clamped, ridged_fbm, saturate,
-    smootherstep01, smoothstep01,
+    billow_fbm, directional_ripples, domain_warp, fbm, rand01, remap_clamped, ridged_fbm,
+    saturate, smootherstep01, smoothstep01,
 };
 use super::decorations::collect_chunk_decorations;
 use super::donnees::{ChunkState, ResourceDeposit, ResourceKind, TerrainCell};
@@ -20,6 +20,10 @@
     pub ejecta: f32,
     pub wind_streaks: f32,
     pub salts: f32,
+    pub volcanic_mask: f32,
+    pub sediment_mask: f32,
+    pub yardang_mask: f32,
+    pub hematite: f32,
 }
 
 #[derive(Clone, Copy, Debug, Default)]
@@ -31,6 +35,20 @@
     ridge_mask: f64,
     dune_field: f64,
     plateau_mask: f64,
+    highland_mask: f64,
+    volcanic_mask: f64,
+    sediment_mask: f64,
+    yardang_mask: f64,
+    lava_flow: f64,
+    layered_mask: f64,
+}
+
+#[derive(Clone, Copy, Debug, Default)]
+struct ProvinceBlend {
+    highland: f64,
+    volcanic: f64,
+    basin: f64,
+    yardang: f64,
 }
 
 #[derive(Clone, Copy, Debug, Default)]
@@ -55,54 +73,90 @@
     pic_central: f64,
 }
 
-const BANDES_CRATERES: [CraterBand; 4] = [
+impl CraterBand {
+    fn scaled(self, factor: f64) -> Self {
+        let factor = factor.max(1e-4);
+        Self {
+            maille: self.maille / factor,
+            rayon_min: self.rayon_min / factor,
+            rayon_max: self.rayon_max / factor,
+            ..self
+        }
+    }
+}
+
+const BANDES_CRATERES: [CraterBand; 6] = [
     CraterBand {
-        maille: 22.0,
+        maille: 20.0,
         rayon_min: 1.8,
-        rayon_max: 4.4,
-        densite: 0.58,
-        profondeur: 0.20,
-        force_bourrelet: 0.42,
+        rayon_max: 4.0,
+        densite: 0.62,
+        profondeur: 0.18,
+        force_bourrelet: 0.40,
         largeur_bourrelet: 0.16,
-        force_ejecta: 0.08,
+        force_ejecta: 0.06,
         largeur_ejecta: 0.34,
         pic_central: 0.0,
     },
     CraterBand {
-        maille: 54.0,
-        rayon_min: 4.6,
-        rayon_max: 9.8,
-        densite: 0.44,
-        profondeur: 0.48,
-        force_bourrelet: 0.38,
+        maille: 46.0,
+        rayon_min: 4.0,
+        rayon_max: 8.6,
+        densite: 0.46,
+        profondeur: 0.42,
+        force_bourrelet: 0.36,
         largeur_bourrelet: 0.17,
-        force_ejecta: 0.13,
-        largeur_ejecta: 0.44,
+        force_ejecta: 0.10,
+        largeur_ejecta: 0.42,
         pic_central: 0.0,
     },
     CraterBand {
-        maille: 136.0,
-        rayon_min: 9.5,
-        rayon_max: 21.0,
+        maille: 112.0,
+        rayon_min: 8.5,
+        rayon_max: 18.0,
         densite: 0.28,
-        profondeur: 0.92,
-        force_bourrelet: 0.34,
-        largeur_bourrelet: 0.20,
-        force_ejecta: 0.18,
-        largeur_ejecta: 0.52,
-        pic_central: 0.09,
+        profondeur: 0.88,
+        force_bourrelet: 0.33,
+        largeur_bourrelet: 0.19,
+        force_ejecta: 0.14,
+        largeur_ejecta: 0.50,
+        pic_central: 0.06,
     },
     CraterBand {
-        maille: 320.0,
+        maille: 260.0,
         rayon_min: 18.0,
-        rayon_max: 44.0,
+        rayon_max: 40.0,
         densite: 0.18,
-        profondeur: 1.38,
-        force_bourrelet: 0.31,
+        profondeur: 1.30,
+        force_bourrelet: 0.30,
         largeur_bourrelet: 0.22,
-        force_ejecta: 0.22,
+        force_ejecta: 0.18,
         largeur_ejecta: 0.58,
-        pic_central: 0.07,
+        pic_central: 0.08,
+    },
+    CraterBand {
+        maille: 620.0,
+        rayon_min: 40.0,
+        rayon_max: 82.0,
+        densite: 0.10,
+        profondeur: 1.90,
+        force_bourrelet: 0.24,
+        largeur_bourrelet: 0.24,
+        force_ejecta: 0.14,
+        largeur_ejecta: 0.60,
+        pic_central: 0.04,
+    },
+    CraterBand {
+        maille: 1300.0,
+        rayon_min: 78.0,
+        rayon_max: 150.0,
+        densite: 0.05,
+        profondeur: 2.60,
+        force_bourrelet: 0.18,
+        largeur_bourrelet: 0.26,
+        force_ejecta: 0.10,
+        largeur_ejecta: 0.64,
+        pic_central: 0.0,
     },
 ];
 
@@ -122,25 +176,32 @@
                 estimate_normal(seed.0, world_x as f64 + 0.5, world_y as f64 + 0.5, profile);
             let slope = (1.0 - normal.y).clamp(0.0, 1.0);
 
-            let obstacle_chance =
-                (surface.rockiness * 0.060 + surface.ejecta * 0.018 + surface.crater_rim * 0.014
-                    - surface.dust_cover * 0.015)
-                    .clamp(0.0, 0.11);
-            let blocked =
-                slope < 0.92 && rand01(seed.0 ^ 0x5D1E_5A1E, world_x, world_y) < obstacle_chance;
-
-            let ice_probability = (surface.cold_trap * 0.55
-                + surface.crater_floor * 0.22
+            let obstacle_chance = (surface.rockiness * 0.056
+                + surface.ejecta * 0.020
+                + surface.crater_rim * 0.018
+                + surface.yardang_mask * 0.016
+                + surface.volcanic_mask * 0.008
+                - surface.dust_cover * 0.020
+                - surface.sediment_mask * 0.006)
+                .clamp(0.0, 0.12);
+            let blocked = slope < 0.92
+                && rand01(seed.0 ^ 0x5D1E_5A1E, world_x, world_y) < obstacle_chance;
+
+            let ice_probability = (surface.cold_trap * 0.50
+                + surface.crater_floor * 0.16
+                + surface.sediment_mask * 0.10
                 + (1.0 - surface.rockiness) * 0.08)
                 .clamp(0.0, 0.95)
                 * profile.resource_frequency as f32
-                * 2.6;
+                * 2.5;
             let resource = if surface.cold_trap > 0.34
-                && slope < 0.64
+                && slope < 0.66
                 && rand01(seed.0 ^ 0xBADA_551C, world_x, world_y) < ice_probability
             {
-                let richness =
-                    (surface.cold_trap * 6.0 + surface.crater_floor * 4.0).round() as u16;
+                let richness = (surface.cold_trap * 5.0
+                    + surface.crater_floor * 2.0
+                    + surface.sediment_mask * 2.0)
+                    .round() as u16;
                 Some(ResourceDeposit {
                     kind: ResourceKind::Ice,
                     amount: 4
@@ -151,9 +212,10 @@
                 None
             };
 
-            let constructible = slope < 0.78
+            let constructible = slope < 0.80
                 && !blocked
-                && !(surface.crater_rim > 0.80 && surface.rockiness > 0.70);
+                && !(surface.crater_rim > 0.78 && surface.rockiness > 0.64)
+                && !(surface.yardang_mask > 0.82 && surface.rockiness > 0.46);
 
             total_height += surface.height;
             if resource.is_some() {
@@ -210,8 +272,8 @@
     let relief = sample_relief(seed, x, y, profile);
     let dust_noise = fbm(
         seed ^ 0xFADE_CAFE,
-        x * profile.noise_scale * 0.55 + 23.0,
-        y * profile.noise_scale * 0.55 - 17.0,
+        x * profile.noise_scale * 0.48 + 23.0,
+        y * profile.noise_scale * 0.48 - 17.0,
         4,
         2.0,
         0.52,
@@ -219,8 +281,8 @@
         + 0.5;
     let dark_patches = billow_fbm(
         seed ^ 0x0BAD_1DEA,
-        x * profile.noise_scale * 0.24 - 61.0,
-        y * profile.noise_scale * 0.24 + 14.0,
+        x * profile.noise_scale * 0.22 - 61.0,
+        y * profile.noise_scale * 0.22 + 14.0,
         3,
         2.0,
         0.55,
@@ -228,58 +290,79 @@
         + 0.5;
     let salt_noise = fbm(
         seed ^ 0x51A7_11DE,
-        x * profile.noise_scale * 0.14 + 7.0,
-        y * profile.noise_scale * 0.14 - 11.0,
+        x * profile.noise_scale * 0.13 + 7.0,
+        y * profile.noise_scale * 0.13 - 11.0,
         2,
         2.0,
         0.55,
     ) * 0.5
         + 0.5;
 
+    let wind_streaks = (directional_ripples(
+        seed ^ 0xA11C_5F11,
+        x,
+        y,
+        profile.wind_direction.x as f64,
+        profile.wind_direction.y as f64,
+        profile.noise_scale * 17.0,
+        profile.noise_scale * 0.70,
+    ) * (relief.dune_field * 0.55 + relief.sediment_mask * 0.28 + dust_noise * 0.12)) as f32;
+
     let dust_cover = saturate(
-        0.34 + dust_noise * 0.26
-            + relief.crater_floor * 0.24
-            + relief.dune_field * 0.28
-            + relief.plateau_mask * 0.08
-            - relief.crater_rim * 0.40
-            - relief.ejecta * 0.16
-            - relief.ridge_mask * 0.34,
+        0.18 + dust_noise * 0.22
+            + relief.sediment_mask * 0.24
+            + relief.dune_field * 0.30
+            + relief.crater_floor * 0.16
+            + relief.plateau_mask * 0.05
+            - relief.crater_rim * 0.42
+            - relief.ejecta * 0.18
+            - relief.ridge_mask * 0.26
+            - relief.volcanic_mask * 0.08,
     ) as f32;
 
     let rockiness = saturate(
-        0.14 + relief.ridge_mask * 0.56
+        0.08 + relief.ridge_mask * 0.40
             + relief.crater_rim * 0.22
-            + relief.ejecta * 0.24
-            + dark_patches * 0.14
-            - dust_cover as f64 * 0.32,
+            + relief.ejecta * 0.26
+            + relief.highland_mask * 0.20
+            + relief.yardang_mask * 0.18
+            + dark_patches * 0.10
+            - dust_cover as f64 * 0.30
+            - relief.sediment_mask * 0.12,
     ) as f32;
 
-    let basalt_exposure = saturate(
-        0.10 + dark_patches * 0.42 + rockiness as f64 * 0.36 + relief.ejecta * 0.16
-            - dust_cover as f64 * 0.28,
+    let cold_height_mask = remap_clamped(-4.4, 1.4, 1.0, 0.0, relief.height);
+    let cold_trap = saturate(
+        relief.crater_floor * 0.72 + cold_height_mask * 0.22 + relief.sediment_mask * 0.10
+            - relief.crater_rim * 0.16
+            - rockiness as f64 * 0.12,
     ) as f32;
 
-    let wind_streaks = (directional_ripples(
-        seed ^ 0xD00D_5EED,
-        x,
-        y,
-        profile.wind_direction.x as f64,
-        profile.wind_direction.y as f64,
-        profile.noise_scale * 18.0,
-        profile.noise_scale * 0.72,
-    ) * (dust_cover as f64 * 0.7 + relief.dune_field * 0.3)) as f32;
+    let salts = saturate(
+        relief.crater_floor * 0.10
+            + cold_trap as f64 * 0.12
+            + relief.sediment_mask * 0.24
+            + relief.plateau_mask * 0.06
+            + dust_cover as f64 * 0.10 * salt_noise
+            - relief.volcanic_mask * 0.08,
+    ) as f32;
 
-    let cold_height_mask = remap_clamped(-3.8, 1.2, 1.0, 0.0, relief.height);
-    let cold_trap = saturate(
-        relief.crater_floor * 0.78 + cold_height_mask * 0.22
-            - relief.crater_rim * 0.18
-            - rockiness as f64 * 0.10,
+    let basalt_exposure = saturate(
+        0.06 + dark_patches * 0.16
+            + relief.volcanic_mask * 0.44
+            + relief.lava_flow * 0.26
+            + rockiness as f64 * 0.22
+            + relief.ejecta * 0.10
+            - dust_cover as f64 * 0.26
+            - salts as f64 * 0.10,
     ) as f32;
 
-    let salts = saturate(
-        relief.crater_floor * 0.16
-            + cold_trap as f64 * 0.16
-            + dust_cover as f64 * 0.10 * salt_noise,
+    let hematite = saturate(
+        0.04 + relief.sediment_mask * 0.18
+            + wind_streaks as f64 * 0.22
+            + relief.layered_mask * 0.10
+            + relief.crater_floor * 0.06
+            - basalt_exposure as f64 * 0.10,
     ) as f32;
 
     SurfaceSample {
@@ -293,6 +376,10 @@
         ejecta: relief.ejecta as f32,
         wind_streaks,
         salts,
+        volcanic_mask: relief.volcanic_mask as f32,
+        sediment_mask: relief.sediment_mask as f32,
+        yardang_mask: relief.yardang_mask as f32,
+        hematite,
     }
 }
 
@@ -305,35 +392,35 @@
 }
 
 fn sample_relief(seed: u64, x: f64, y: f64, profile: &PlanetProfile) -> ReliefSample {
-    let (macro_x, macro_y) = domain_warp(seed ^ 0x11A1_900D, x, y, profile.noise_scale * 0.42, 7.4);
+    let (macro_x, macro_y) = domain_warp(seed ^ 0x11A1_900D, x, y, profile.noise_scale * 0.30, 15.0);
     let (detail_x, detail_y) =
-        domain_warp(seed ^ 0x9154_C7A1, x, y, profile.noise_scale * 0.92, 3.1);
+        domain_warp(seed ^ 0x9154_C7A1, x, y, profile.noise_scale * 1.02, 3.4);
 
     let broad = fbm(
         seed ^ 0x6A09_E667,
-        macro_x * profile.noise_scale * 0.42,
-        macro_y * profile.noise_scale * 0.42,
+        macro_x * profile.noise_scale * 0.36,
+        macro_y * profile.noise_scale * 0.36,
         5,
-        2.04,
+        2.02,
         0.52,
     );
     let plateaus = billow_fbm(
         seed ^ 0xBB67_AE85,
-        macro_x * profile.noise_scale * 0.24,
-        macro_y * profile.noise_scale * 0.24,
+        macro_x * profile.noise_scale * 0.20,
+        macro_y * profile.noise_scale * 0.20,
         4,
         2.0,
         0.55,
     );
     let ridges = ridged_fbm(
         seed ^ 0x3C6E_F372,
-        detail_x * profile.noise_scale * 1.20,
-        detail_y * profile.noise_scale * 1.20,
+        detail_x * profile.noise_scale * 1.06,
+        detail_y * profile.noise_scale * 1.06,
         4,
-        2.08,
+        2.06,
         0.55,
     );
-    let basin = fbm(
+    let basin_noise = billow_fbm(
         seed ^ 0xA54F_F53A,
         x * profile.noise_scale * 0.16 - 11.0,
         y * profile.noise_scale * 0.16 + 7.0,
@@ -341,51 +428,107 @@
         2.0,
         0.55,
     );
+    let lava_flow = billow_fbm(
+        seed ^ 0x0FF1_0A77,
+        macro_x * profile.noise_scale * 0.46 - 7.0,
+        macro_y * profile.noise_scale * 0.46 + 13.0,
+        4,
+        2.0,
+        0.53,
+    );
+    let layered = billow_fbm(
+        seed ^ 0x67AE_8584,
+        detail_x * profile.noise_scale * 0.84 + 43.0,
+        detail_y * profile.noise_scale * 0.84 - 21.0,
+        3,
+        2.0,
+        0.55,
+    );
+
+    let province = province_blend(seed, x, y, profile);
+    let highland_mask = smoothstep01(remap_clamped(0.08, 0.26, 0.0, 1.0, province.highland));
+    let volcanic_mask = smoothstep01(remap_clamped(0.22, 0.52, 0.0, 1.0, province.volcanic));
+    let sediment_mask = smoothstep01(remap_clamped(0.08, 0.28, 0.0, 1.0, province.basin));
+    let yardang_mask = smoothstep01(remap_clamped(0.18, 0.40, 0.0, 1.0, province.yardang));
 
     let plateau_mask = smootherstep01(remap_clamped(
-        -0.08,
-        0.46,
+        -0.10,
+        0.44,
+        0.0,
+        1.0,
+        plateaus * 0.60 + basin_noise * 0.14 + highland_mask * 0.16,
+    ));
+    let ridge_mask = smoothstep01(remap_clamped(
+        0.32,
+        0.80,
         0.0,
         1.0,
-        plateaus * 0.65 + basin * 0.35,
+        ridges + highland_mask * 0.10 + yardang_mask * 0.15,
     ));
-    let ridge_mask = smoothstep01(remap_clamped(0.36, 0.82, 0.0, 1.0, ridges));
     let dune_field = smoothstep01(remap_clamped(
-        -0.12,
-        0.38,
+        -0.18,
+        0.34,
         0.0,
         1.0,
-        basin * 0.45 + broad * 0.20,
+        basin_noise * 0.34 + sediment_mask * 0.58 + broad * 0.08 - highland_mask * 0.16,
     ));
 
     let aeolian_relief = (directional_ripples(
-        seed ^ 0x1F83_D9AB,
+        seed ^ 0xD00D_5EED,
+        x,
+        y,
+        profile.wind_direction.x as f64,
+        profile.wind_direction.y as f64,
+        profile.noise_scale * 14.5,
+        profile.noise_scale * 0.68,
+    ) - 0.5)
+        * (0.12 + dune_field * 0.52);
+
+    let yardang_relief = (directional_ripples(
+        seed ^ 0x4E6F_7274,
         x,
         y,
         profile.wind_direction.x as f64,
         profile.wind_direction.y as f64,
-        profile.noise_scale * 13.0,
-        profile.noise_scale * 0.66,
+        profile.noise_scale * 7.0,
+        profile.noise_scale * 0.40,
     ) - 0.5)
-        * 0.42
-        * dune_field;
+        * (0.08 + yardang_mask * 1.00);
+
+    let wrinkle_ridges = (directional_ripples(
+        seed ^ 0x7788_99AA,
+        x,
+        y,
+        0.54,
+        -0.84,
+        profile.noise_scale * 5.0,
+        profile.noise_scale * 0.22,
+    ) - 0.5)
+        * (0.06 + volcanic_mask * 0.40);
 
     let crater = sample_craters(seed, x, y, profile);
+    let crater_weight = 0.76 + highland_mask * 0.48 + sediment_mask * 0.16 + yardang_mask * 0.12
+        - volcanic_mask * 0.20;
     let micro_relief = fbm(
         seed ^ 0x5BE0_CD19,
-        detail_x * profile.noise_scale * 2.85 + 91.0,
-        detail_y * profile.noise_scale * 2.85 - 37.0,
+        detail_x * profile.noise_scale * 2.65 + 91.0,
+        detail_y * profile.noise_scale * 2.65 - 37.0,
         2,
         2.0,
         0.5,
-    ) * 0.10;
+    ) * (0.08 + highland_mask * 0.04 + yardang_mask * 0.03);
 
-    let height = broad * 2.55
-        + (plateau_mask * 2.0 - 1.0) * 0.85
-        + (ridges * 2.0 - 1.0) * 0.58
-        + basin * 0.55
+    let basin_depth = sediment_mask * (0.50 + (basin_noise * 0.5 + 0.5) * 1.25);
+    let height = broad * (1.90 + highland_mask * 0.62 - sediment_mask * 0.06)
+        + (plateau_mask * 2.0 - 1.0) * (0.30 + highland_mask * 0.66 + yardang_mask * 0.10)
+        + (ridges * 2.0 - 1.0) * (0.16 + highland_mask * 0.40 + yardang_mask * 0.18)
+        - basin_depth * 1.25
+        + lava_flow * (0.12 + volcanic_mask * 1.00)
+        + layered * sediment_mask * 0.24
         + aeolian_relief
-        + crater.height
+        + yardang_relief
+        + wrinkle_ridges
+        + crater.height * crater_weight
         + micro_relief;
 
     ReliefSample {
@@ -396,17 +539,75 @@
         ridge_mask,
         dune_field,
         plateau_mask,
+        highland_mask,
+        volcanic_mask,
+        sediment_mask,
+        yardang_mask,
+        lava_flow: (lava_flow * 0.5 + 0.5).max(0.0),
+        layered_mask: (layered * 0.5 + 0.5).max(0.0),
+    }
+}
+
+fn province_blend(seed: u64, x: f64, y: f64, profile: &PlanetProfile) -> ProvinceBlend {
+    let (province_x, province_y) =
+        domain_warp(seed ^ 0x72AA_5511, x, y, profile.noise_scale * 0.09, 32.0);
+
+    let a = fbm(
+        seed ^ 0x510E_527F,
+        province_x * profile.noise_scale * 0.08 + 18.0,
+        province_y * profile.noise_scale * 0.08 - 31.0,
+        4,
+        2.0,
+        0.54,
+    );
+    let b = billow_fbm(
+        seed ^ 0x9B05_688C,
+        province_x * profile.noise_scale * 0.06 - 27.0,
+        province_y * profile.noise_scale * 0.06 + 14.0,
+        4,
+        2.0,
+        0.55,
+    );
+    let c = ridged_fbm(
+        seed ^ 0x1F83_D9AB,
+        province_x * profile.noise_scale * 0.14 + 9.0,
+        province_y * profile.noise_scale * 0.14 + 5.0,
+        3,
+        2.04,
+        0.55,
+    );
+    let d = fbm(
+        seed ^ 0x428A_2F98,
+        province_x * profile.noise_scale * 0.11 - 7.0,
+        province_y * profile.noise_scale * 0.11 + 27.0,
+        4,
+        2.0,
+        0.55,
+    );
+
+    let highland = (0.55 + b * 0.72 + c * 0.18 - a * 0.12).max(0.05);
+    let volcanic = (0.48 + a * 0.78 - b * 0.12 - c * 0.06 + d * 0.10).max(0.05);
+    let basin = (0.40 - a * 0.40 + b * 0.18 + d * 0.75).max(0.05);
+    let yardang = (0.10 + c * 0.50 + d * 0.12 - b * 0.08).max(0.02);
+    let total = (highland + volcanic + basin + yardang).max(1e-6);
+
+    ProvinceBlend {
+        highland: highland / total,
+        volcanic: volcanic / total,
+        basin: basin / total,
+        yardang: yardang / total,
     }
 }
 
 fn sample_craters(seed: u64, x: f64, y: f64, profile: &PlanetProfile) -> CraterContribution {
     let mut total = CraterContribution::default();
+    let crater_scale = (profile.crater_scale / 0.052).clamp(0.55, 1.85);
 
     for (band_index, band) in BANDES_CRATERES.into_iter().enumerate() {
         let band_seed = seed
-            ^ (profile.crater_scale.to_bits() as u64)
+            ^ profile.crater_scale.to_bits()
             ^ (band_index as u64 + 1).wrapping_mul(0x9E37_79B9_7F4A_7C15);
-        let crater = sample_crater_band(band_seed, x, y, band);
+        let crater = sample_crater_band(band_seed, x, y, band.scaled(crater_scale));
         total.height += crater.height;
         total.floor = total.floor.max(crater.floor);
         total.rim = total.rim.max(crater.rim);
diff --git a/src/world/maillage.rs b/src/world/maillage.rs
--- a/src/world/maillage.rs	2026-03-14 21:28:36.859034797 +0000
+++ b/src/world/maillage.rs	2026-03-14 21:17:57.084135966 +0000
@@ -4,64 +4,64 @@
 use bevy::render::render_resource::PrimitiveTopology;
 
 use super::donnees::ChunkState;
-use super::generation::sample_surface;
+use super::generation::{sample_height, sample_surface};
 use super::{CHUNK_SIZE_CELLS, PlanetProfile, WorldSeed};
 
+const TERRAIN_MESH_SUBDIVISIONS: u32 = 2;
+
 pub(crate) fn build_chunk_mesh(
     chunk: &ChunkState,
     profile: &PlanetProfile,
     seed: WorldSeed,
 ) -> Mesh {
-    let side = (CHUNK_SIZE_CELLS + 1) as usize;
+    let mesh_cells = CHUNK_SIZE_CELLS as u32 * TERRAIN_MESH_SUBDIVISIONS;
+    let side = mesh_cells as usize + 1;
+    let step_cells = 1.0 / TERRAIN_MESH_SUBDIVISIONS as f64;
+    let step_meters = profile.cell_size_meters / TERRAIN_MESH_SUBDIVISIONS as f32;
+
     let mut positions = Vec::with_capacity(side * side);
     let mut normals = Vec::with_capacity(side * side);
     let mut colors = Vec::with_capacity(side * side);
     let mut uvs = Vec::with_capacity(side * side);
-    let mut indices = Vec::with_capacity(CHUNK_SIZE_CELLS as usize * CHUNK_SIZE_CELLS as usize * 6);
+    let mut indices = Vec::with_capacity(mesh_cells as usize * mesh_cells as usize * 6);
 
-    for y in 0..=CHUNK_SIZE_CELLS {
-        for x in 0..=CHUNK_SIZE_CELLS {
-            let height = chunk.vertex_heights[y as usize * side + x as usize];
-            let left_x = if x == 0 { 0 } else { x - 1 };
-            let right_x = if x == CHUNK_SIZE_CELLS {
-                CHUNK_SIZE_CELLS
-            } else {
-                x + 1
-            };
-            let down_y = if y == 0 { 0 } else { y - 1 };
-            let up_y = if y == CHUNK_SIZE_CELLS {
-                CHUNK_SIZE_CELLS
-            } else {
-                y + 1
-            };
-
-            let left = chunk.vertex_heights[y as usize * side + left_x as usize];
-            let right = chunk.vertex_heights[y as usize * side + right_x as usize];
-            let down = chunk.vertex_heights[down_y as usize * side + x as usize];
-            let up = chunk.vertex_heights[up_y as usize * side + x as usize];
-            let normal =
-                Vec3::new(left - right, 2.0 * profile.cell_size_meters, down - up).normalize();
+    for y in 0..=mesh_cells {
+        for x in 0..=mesh_cells {
+            let world_x_cell =
+                chunk.coord.x as f64 * CHUNK_SIZE_CELLS as f64 + x as f64 * step_cells;
+            let world_y_cell =
+                chunk.coord.y as f64 * CHUNK_SIZE_CELLS as f64 + y as f64 * step_cells;
+
+            let height = sample_height(seed.0, world_x_cell, world_y_cell, profile);
+            let left = sample_height(seed.0, world_x_cell - step_cells, world_y_cell, profile);
+            let right = sample_height(seed.0, world_x_cell + step_cells, world_y_cell, profile);
+            let down = sample_height(seed.0, world_x_cell, world_y_cell - step_cells, profile);
+            let up = sample_height(seed.0, world_x_cell, world_y_cell + step_cells, profile);
+            let normal = Vec3::new(left - right, 2.0 * step_meters, down - up).normalize();
             let slope = (1.0 - normal.y).clamp(0.0, 1.0);
-            let world_x = chunk.coord.x * CHUNK_SIZE_CELLS + x;
-            let world_y = chunk.coord.y * CHUNK_SIZE_CELLS + y;
-            let surface = sample_surface(seed.0, world_x as f64, world_y as f64, profile);
+            let surface = sample_surface(seed.0, world_x_cell, world_y_cell, profile);
+            let curvature = ((left + right + down + up) - 4.0 * height)
+                / profile.cell_size_meters.max(0.0001)
+                / TERRAIN_MESH_SUBDIVISIONS as f32;
 
-            positions.push([
-                x as f32 * profile.cell_size_meters,
-                height,
-                y as f32 * profile.cell_size_meters,
-            ]);
+            positions.push([x as f32 * step_meters, height, y as f32 * step_meters]);
             normals.push([normal.x, normal.y, normal.z]);
-            colors.push(terrain_vertex_color(surface, height, slope, profile));
+            colors.push(terrain_vertex_color(
+                surface,
+                height,
+                slope,
+                curvature,
+                profile,
+            ));
             uvs.push([
-                x as f32 / CHUNK_SIZE_CELLS as f32,
-                y as f32 / CHUNK_SIZE_CELLS as f32,
+                x as f32 / mesh_cells.max(1) as f32,
+                y as f32 / mesh_cells.max(1) as f32,
             ]);
         }
     }
 
-    for y in 0..CHUNK_SIZE_CELLS {
-        for x in 0..CHUNK_SIZE_CELLS {
+    for y in 0..mesh_cells {
+        for x in 0..mesh_cells {
             let i0 = (y as usize * side + x as usize) as u32;
             let i1 = i0 + 1;
             let i2 = i0 + side as u32;
@@ -85,57 +85,65 @@
     surface: super::generation::SurfaceSample,
     height: f32,
     slope: f32,
+    curvature: f32,
     profile: &PlanetProfile,
 ) -> [f32; 4] {
-    let height_factor = ((height + 5.0) / 10.0).clamp(0.0, 1.0);
+    let height_factor = ((height + 6.0) / 12.0).clamp(0.0, 1.0);
     let slope_factor = slope.clamp(0.0, 1.0);
+    let convexity = (-curvature * 0.70).clamp(0.0, 1.0);
+    let concavity = (curvature * 0.70).clamp(0.0, 1.0);
 
     let mut color = blend_color(
         profile.palette.ground_shadow,
         profile.palette.ground_mid,
-        0.28 + surface.dust_cover * 0.48,
+        0.18 + surface.dust_cover * 0.28 + surface.sediment_mask * 0.10,
     );
     color = blend_color(
         color,
-        profile.palette.accent,
-        0.10 + surface.crater_rim * 0.10,
+        profile.palette.basalt_exposed,
+        surface.basalt_exposure * (0.20 + surface.volcanic_mask * 0.26) + slope_factor * 0.08,
     );
     color = blend_color(
         color,
-        profile.palette.oxide_dark,
-        surface.wind_streaks * 0.18 + surface.basalt_exposure * 0.05,
+        profile.palette.rock_dark,
+        surface.rockiness * 0.24 + surface.crater_rim * 0.12 + surface.yardang_mask * 0.10,
     );
     color = blend_color(
         color,
-        profile.palette.basalt_exposed,
-        surface.basalt_exposure * 0.34 + slope_factor * 0.10,
+        profile.palette.oxide_dark,
+        surface.hematite * 0.24 + surface.wind_streaks * 0.14,
     );
     color = blend_color(
         color,
-        profile.palette.rock_dark,
-        surface.rockiness * 0.26 + surface.crater_rim * 0.14,
+        profile.palette.salt_light,
+        surface.salts * 0.30 + surface.sediment_mask * 0.08,
     );
     color = blend_color(
         color,
         profile.palette.dust_light,
-        surface.dust_cover * 0.34 + surface.wind_streaks * 0.14,
+        surface.dust_cover * 0.42 + surface.wind_streaks * 0.12,
     );
     color = blend_color(
         color,
-        profile.palette.salt_light,
-        surface.salts * 0.24 + surface.crater_floor * 0.06,
+        profile.palette.accent,
+        surface.crater_rim * 0.08 + surface.yardang_mask * 0.06,
     );
     color = blend_color(
         color,
         profile.palette.ground_highlight,
-        height_factor * 0.36 + surface.dust_cover * 0.06,
+        height_factor * 0.24 + convexity * 0.18 + surface.sediment_mask * 0.06,
+    );
+    color = blend_color(
+        color,
+        profile.palette.ground_shadow,
+        concavity * 0.18 + surface.crater_floor * 0.06,
     );
 
-    if surface.cold_trap > 0.72 && height < -1.5 {
+    if surface.cold_trap > 0.72 && height < -2.2 {
         color = blend_color(
             color,
             profile.palette.ice,
-            ((surface.cold_trap - 0.72) / 0.28).clamp(0.0, 1.0) * 0.28,
+            ((surface.cold_trap - 0.72) / 0.28).clamp(0.0, 1.0) * 0.32,
         );
     }
 
diff --git a/src/world/parametres.rs b/src/world/parametres.rs
--- a/src/world/parametres.rs	2026-03-14 21:28:36.859282186 +0000
+++ b/src/world/parametres.rs	2026-03-14 21:12:44.941595053 +0000
@@ -45,24 +45,25 @@
             chunk_size_cells: CHUNK_SIZE_CELLS,
             active_radius: ACTIVE_CHUNK_RADIUS,
             origin_recenter_distance: CELL_SIZE_METERS * CHUNK_SIZE_CELLS as f32 * 1.75,
-            // Je garde ces deux valeurs car elles servent de base de calibration globale.
-            // Toute la synthese du relief derive ensuite de ces frequences.
-            noise_scale: 0.038,
+            // Cette echelle reste volontairement assez petite pour laisser vivre de grands
+            // ensembles geomorphologiques visibles sur plusieurs chunks.
+            noise_scale: 0.036,
+            // La valeur sert de coefficient de calibration pour les bandes de crateres.
             crater_scale: 0.052,
             resource_frequency: 0.118,
-            // Direction dominante des formes eoliennes pour donner une lecture visuelle cohérente.
-            wind_direction: Vec2::new(0.83, 0.56).normalize(),
+            // Direction dominante des formes eoliennes (dunes / yardangs) pour une lecture forte.
+            wind_direction: Vec2::new(0.87, 0.49).normalize(),
             palette: PlanetPalette {
-                ground_shadow: Color::srgb(0.27, 0.15, 0.11),
-                ground_mid: Color::srgb(0.56, 0.32, 0.20),
-                ground_highlight: Color::srgb(0.76, 0.53, 0.36),
-                dust_light: Color::srgb(0.84, 0.66, 0.49),
-                rock_dark: Color::srgb(0.18, 0.13, 0.12),
+                ground_shadow: Color::srgb(0.22, 0.12, 0.10),
+                ground_mid: Color::srgb(0.54, 0.31, 0.20),
+                ground_highlight: Color::srgb(0.79, 0.58, 0.42),
+                dust_light: Color::srgb(0.88, 0.70, 0.55),
+                rock_dark: Color::srgb(0.16, 0.13, 0.12),
                 ice: Color::srgb(0.83, 0.89, 0.93),
-                accent: Color::srgb(0.69, 0.28, 0.17),
-                basalt_exposed: Color::srgb(0.24, 0.18, 0.17),
-                oxide_dark: Color::srgb(0.43, 0.20, 0.14),
-                salt_light: Color::srgb(0.90, 0.83, 0.73),
+                accent: Color::srgb(0.67, 0.30, 0.18),
+                basalt_exposed: Color::srgb(0.21, 0.18, 0.17),
+                oxide_dark: Color::srgb(0.46, 0.21, 0.15),
+                salt_light: Color::srgb(0.92, 0.85, 0.78),
             },
         }
     }
diff --git a/src/world/streaming.rs b/src/world/streaming.rs
--- a/src/world/streaming.rs	2026-03-14 21:28:36.859446280 +0000
+++ b/src/world/streaming.rs	2026-03-14 21:23:38.512658097 +0000
@@ -39,6 +39,7 @@
     terrain_material: Handle<StandardMaterial>,
     rock_dark_material: Handle<StandardMaterial>,
     rock_dust_material: Handle<StandardMaterial>,
+    rock_salt_material: Handle<StandardMaterial>,
 }
 
 #[derive(SystemParam)]
@@ -62,26 +63,30 @@
 ) {
     commands.insert_resource(WorldVisualAssets {
         terrain_material: materials.add(StandardMaterial {
-            // Le mesh porte deja la couleur du terrain via les vertex colors.
-            // Je garde donc un materiau tres neutre pour ne pas ecraser ce travail.
             base_color: Color::srgb(1.0, 1.0, 1.0),
-            perceptual_roughness: 0.98,
+            perceptual_roughness: 0.97,
+            reflectance: 0.08,
             metallic: 0.0,
             ..default()
         }),
         rock_dark_material: materials.add(StandardMaterial {
-            base_color: blend_color(
-                profile.palette.rock_dark,
-                profile.palette.basalt_exposed,
-                0.18,
-            ),
-            perceptual_roughness: 1.0,
+            base_color: blend_color(profile.palette.rock_dark, profile.palette.basalt_exposed, 0.24),
+            perceptual_roughness: 0.99,
+            reflectance: 0.06,
             metallic: 0.0,
             ..default()
         }),
         rock_dust_material: materials.add(StandardMaterial {
-            base_color: blend_color(profile.palette.ground_mid, profile.palette.dust_light, 0.64),
-            perceptual_roughness: 1.0,
+            base_color: blend_color(profile.palette.ground_mid, profile.palette.dust_light, 0.68),
+            perceptual_roughness: 0.98,
+            reflectance: 0.09,
+            metallic: 0.0,
+            ..default()
+        }),
+        rock_salt_material: materials.add(StandardMaterial {
+            base_color: blend_color(profile.palette.salt_light, profile.palette.dust_light, 0.18),
+            perceptual_roughness: 0.92,
+            reflectance: 0.16,
             metallic: 0.0,
             ..default()
         }),
@@ -290,7 +295,11 @@
 ) -> Vec<Entity> {
     let mut entities = Vec::new();
 
-    for material_kind in [DecorationMaterial::Dark, DecorationMaterial::Dust] {
+    for material_kind in [
+        DecorationMaterial::Dark,
+        DecorationMaterial::Dust,
+        DecorationMaterial::Salt,
+    ] {
         let Some(mesh) = build_chunk_decoration_mesh(chunk.decoration_specs(), material_kind)
         else {
             continue;
@@ -299,6 +308,7 @@
         let material = match material_kind {
             DecorationMaterial::Dark => assets.rock_dark_material.clone(),
             DecorationMaterial::Dust => assets.rock_dust_material.clone(),
+            DecorationMaterial::Salt => assets.rock_salt_material.clone(),
         };
 
         let entity = commands
